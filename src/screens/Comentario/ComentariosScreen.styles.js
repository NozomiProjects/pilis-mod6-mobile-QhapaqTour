import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'column',
      },
  
  itemComnetarioBtnCard: {
    position: 'absolute',
   alignItems:'flex-end',
    right: 0,
    height: 100, // Ajusta la altura de la tarjeta fija según tus necesidades
    backgroundColor: 'white', // Cambia el color de fondo según tus necesidades
    // Otros estilos de la tarjeta fija
  },
});